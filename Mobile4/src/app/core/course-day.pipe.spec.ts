import { CourseDayPipe } from './course-day.pipe';

describe('CourseDayPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseDayPipe();
    expect(pipe).toBeTruthy();
  });
});
